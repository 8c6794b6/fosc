#!/bin/sh
#|-*- mode: lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#

(ros:ensure-asdf)
(ql:quickload '(:fosc/benchmarks) :silent t)

(defpackage :ros.script.run-bench
  (:use :cl))

(in-package :ros.script.run-bench)

(defun main (&rest argv)
  (declare (ignore argv))
  (fosc/benchmarks:run))
